# 云函数目录

此目录用于存放微信云开发云函数。

## 使用方式

1. 在微信开发者工具中，右键此目录 → "新建 Node.js 云函数"
2. 编写云函数逻辑后，右键云函数目录 → "上传并部署：云端安装依赖"

## 预设云函数（后续扩展）

- `getUserProfile` — 获取用户信息
- `submitFeedback` — 提交反馈
- `getArticles` — 获取发现页文章列表
